{% extends 'StingerSoftDatatableBundle:Filter:filter.json.twig' %}
{% block additional_fields %}
{% if filter.vars.data is not same as(null) and filter.vars.data is not same as(false) and filter.vars.data is not same as(true) %}
"data": {{ filter.vars.data|json_encode|raw }},
{% endif %}
{% endblock additional_fields %}